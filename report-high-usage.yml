- hosts: localhost
  gather_facts: false

  vars:
    project_id: "joey-preprod-project"
    zone: "asia-southeast2-a"
    region: "asia-southeast2"
    teams_webhook_url: "https://default4d237fc5b1dd42efa9e264887cb44b.21.environment.api.powerplatform.com:443/powerautomate/automations/direct/workflows/4853a33866174df5a29410585f0e9cf5/triggers/manual/paths/invoke?api-version=1&sp=%2Ftriggers%2Fmanual%2Frun&sv=1.0&sig=8E44F_-FX_eJN1eLrAYmL-klR-9puxICRAv2IkzV7cA"

  tasks:

  ###############################################################
  # AUTH
  ###############################################################

  - name: Write GCP SA
    copy:
      content: "{{ gcp_sa_key }}"
      dest: /tmp/gcp-sa.json
      mode: '0600'

  - name: Authenticate
    command: gcloud auth activate-service-account --key-file=/tmp/gcp-sa.json
    changed_when: false

  ###############################################################
  # VM MACHINE TYPE RECOMMENDER (HIGH USAGE = SCALE UP)
  ###############################################################

  - name: Get VM recommendations
    command: >
      gcloud recommender recommendations list
      --project {{ project_id }}
      --location {{ zone }}
      --recommender google.compute.instance.MachineTypeRecommender
      --format=json
    register: vm_raw
    changed_when: false
    failed_when: false

  - name: Parse VM recommendations
    set_fact:
      vm_recs: "{{ vm_raw.stdout | default('[]') | from_json }}"

  - name: Init VM high usage list
    set_fact:
      vm_high: []

  - name: Get current VM machine types
    command: >
      gcloud compute instances describe
      {{ item.content.operationGroups[0].operations[0].resource.split('/')[-1] }}
      --zone {{ zone }}
      --project {{ project_id }}
      --format="value(machineType)"
    register: current_vm
    loop: "{{ vm_recs }}"
    changed_when: false
    failed_when: false

  - name: Append VM scale-up only
    set_fact:
      vm_high: "{{ vm_high + [ {
        'instance': item.item.content.operationGroups[0].operations[0].resource.split('/')[-1],
        'current': item.stdout.split('/')[-1],
        'suggested': item.item.content.operationGroups[0].operations[0].value.machineType.split('/')[-1]
      } ] }}"
    loop: "{{ current_vm.results }}"
    when: >
      item.stdout is defined and
      (
        item.stdout.split('/')[-1].split('-')[-1] | int
      )
      <
      (
        item.item.content.operationGroups[0].operations[0].value.machineType.split('/')[-1] | int
      )

  ###############################################################
  # CLOUD SQL PERFORMANCE RECOMMENDER
  ###############################################################

  - name: Get SQL recommendations
    command: >
      gcloud recommender recommendations list
      --project {{ project_id }}
      --location {{ region }}
      --recommender google.cloudsql.instance.PerformanceRecommender
      --format=json
    register: sql_raw
    changed_when: false
    failed_when: false

  - name: Parse SQL recommendations
    set_fact:
      sql_recs: "{{ sql_raw.stdout | default('[]') | from_json }}"

  - name: Init SQL high usage list
    set_fact:
      sql_high: []

  - name: Append SQL high usage
    set_fact:
      sql_high: "{{ sql_high + [ {
        'instance': item.content.overview.instanceName,
        'engine': item.content.overview.databaseEngine,
        'priority': item.priority,
        'severity': (
          '🔥 Critical' if item.priority == 'P1'
          else '⚠ High' if item.priority == 'P2'
          else 'ℹ Medium'
        ),
        'short_issue': item.description.split('.')[0] ~ '.',
        'console_link': 'https://console.cloud.google.com/sql/instances/' ~ item.content.overview.instanceName ~ '/overview?project=' ~ project_id
      } ] }}"

  ###############################################################
  # BUILD MESSAGE
  ###############################################################

  - name: Set execution timestamp
    set_fact:
      exec_time: "{{ lookup('pipe', 'TZ=Asia/Jakarta date +\"%d-%m-%Y %H:%M:%S WIB\"') }}"
    
  - name: Build Teams message
    set_fact:
      teams_message: |
        🚨 <b>GCP HIGH USAGE ALERT</b><br>
        <b>Project:</b> {{ project_id }}<br>
        <b>Detected at:</b> {{ exec_time }}
  
        <b>━━━━━━━━━━━━━━━━━━━━━━</b><br>
  
        <b>🖥 VM SCALE-UP</b><br>
        {% if vm_high | length == 0 %}
        ✅ No VM experiencing high CPU pressure.<br><br>
        {% else %}
        {% for v in vm_high %}
        🔥 <b>{{ v.instance }}</b><br>
        • Current Type: {{ v.current }}<br>
        • Suggested Type: {{ v.suggested }}<br>
        • Impact: Instance may be CPU constrained.<br><br>
        {% endfor %}
        {% endif %}
  
        <b>━━━━━━━━━━━━━━━━━━━━━━</b><br>
  
        <b>🗄 CLOUD SQL PERFORMANCE</b><br>
        {% if sql_high | length == 0 %}
        ✅ No SQL performance risk detected.<br><br>
        {% else %}
        {% for s in sql_high %}
        ⚠ <b>{{ s.instance }}</b><br>
        • Priority: <b>{{ s.priority }}</b><br>
        • Engine: {{ s.engine }}<br>
        • Issue: {{ s.short_issue }}<br>
        • Recommendation: Review memory configuration.<br>
        {% if s.link is defined %}
        • Console: <a href="{{ s.link }}">Open in GCP</a><br>
        {% endif %}
        <br>
        {% endfor %}
        {% endif %}
  
        <b>━━━━━━━━━━━━━━━━━━━━━━</b><br>
        📊 <b>Summary</b><br>
        • VM High Usage: {{ vm_high | length }}<br>
        • SQL High Usage: {{ sql_high | length }}<br><br>
  
        ℹ <i>Review resources marked with 🔥 or ⚠ for immediate investigation.</i>



  ###############################################################
  # SEND TO TEAMS (Power Automate)
  ###############################################################

  - name: Send to Teams
    uri:
      url: "{{ teams_webhook_url }}"
      method: POST
      body_format: json
      body:
        text: "{{ teams_message }}"
      status_code: 202
