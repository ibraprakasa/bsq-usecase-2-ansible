- name: GCP monitoring listener
  hosts: localhost

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
        
  rules:
    - name: alert detected
      condition: true
      action:
        run_job_template:
          name: hello_world
