- name: GCP monitoring listener
  hosts: localhost

  sources:
    - ansible.eda.generic:
        run_command:
          command: python3 gcp_alert_listener.py
          parse_json: true

  rules:
    - name: alert detected
      condition: true
      action:
        debug:
          msg: "{{ event }}"
