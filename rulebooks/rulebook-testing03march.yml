- name: GCP Alert Listener
  hosts: localhost

  sources:
    - google.cloud.pubsub:
        project_id: orms-sta-project
        subscription: cloudsql-alert-sub

  rules:
    - name: debug event
      condition: true
      action:
        debug:
          msg: "{{ event }}"

          
