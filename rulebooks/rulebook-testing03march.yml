- name: GCP monitoring listener
  hosts: localhost

  sources:
    - ansible.eda.generic:
        payload:
          command: python3 gcp_alert_listener.py

  rules:
    - name: alert detected
      condition: true
      action:
        debug:
          msg: "{{ event }}"
