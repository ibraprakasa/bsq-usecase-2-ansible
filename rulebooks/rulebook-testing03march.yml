- name: GCP monitoring listener
  hosts: localhost

  sources:
    - ansible.eda.generic:
        run: python3 gcp_alert_listener.py
        env:
          GOOGLE_APPLICATION_CREDENTIALS: gcp-sa.json

  rules:
    - name: alert detected
      condition: true
      action:
        debug:
          msg: "{{ event }}"
